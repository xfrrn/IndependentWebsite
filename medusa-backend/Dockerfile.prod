ARG NODE_IMAGE=docker.m.daocloud.io/library/node:20-bookworm
FROM ${NODE_IMAGE}

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

ENV NODE_ENV=production
EXPOSE 9000

CMD ["npm", "run", "start"]
