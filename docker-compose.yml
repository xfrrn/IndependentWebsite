services:
  medusa-db:
    image: postgres:15-alpine
    container_name: medusa-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: medusa
      POSTGRES_PASSWORD: medusa_password
      POSTGRES_DB: medusa_db
    volumes:
      - medusa-db-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U medusa -d medusa_db"]
      interval: 5s
      timeout: 5s
      retries: 20

  medusa-redis:
    image: redis:7-alpine
    container_name: medusa-redis
    restart: unless-stopped
    volumes:
      - medusa-redis-data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 5s
      retries: 20

  medusa-backend:
    build:
      context: ./medusa-backend
      dockerfile: Dockerfile
      args:
        NODE_IMAGE: ${NODE_IMAGE:-docker.m.daocloud.io/library/node:20-bookworm}
    container_name: medusa-backend
    restart: unless-stopped
    depends_on:
      medusa-db:
        condition: service_healthy
      medusa-redis:
        condition: service_healthy
    environment:
      NODE_ENV: development
      HOST: 0.0.0.0
      PORT: 9000
      DATABASE_URL: postgres://medusa:medusa_password@medusa-db:5432/medusa_db?sslmode=disable
      PGSSLMODE: disable
      REDIS_URL: redis://medusa-redis:6379
      STORE_CORS: http://localhost:8000,http://127.0.0.1:8000,http://medusa-frontend:8000
      ADMIN_CORS: http://localhost:9000,http://127.0.0.1:9000,http://medusa-backend:9000
      AUTH_CORS: http://localhost:8000,http://127.0.0.1:8000,http://localhost:9000,http://127.0.0.1:9000,http://medusa-frontend:8000,http://medusa-backend:9000
      JWT_SECRET: supersecret
      COOKIE_SECRET: supersecret
      CONTENT_ADMIN_SECRET: local-content-secret
      STOREFRONT_BASE_URL: http://localhost:8000
      DEFAULT_STORE_COUNTRY_CODE: dk
      ENABLE_DEV_PUBLISHABLE_KEY_ENDPOINT: "true"
      HMR_BIND_HOST: 0.0.0.0
      HMR_HOST: localhost
      HMR_PORT: 9001
      HMR_CLIENT_PORT: 9001
      HMR_PROTOCOL: ws
      AUTO_MIGRATE: "true"
      AUTO_SEED: "true"
      AUTO_ADMIN_USER: "true"
      MEDUSA_ADMIN_EMAIL: ${MEDUSA_ADMIN_EMAIL:-admin@example.com}
      MEDUSA_ADMIN_PASSWORD: ${MEDUSA_ADMIN_PASSWORD:-supersecret}
    ports:
      - "9000:9000"
      - "9001:9001"
    volumes:
      - ./medusa-backend:/app
      - medusa-backend-node-modules:/app/node_modules
      - medusa-backend-build:/app/.medusa

  medusa-frontend:
    build:
      context: ./medusa-frontend
      dockerfile: Dockerfile
      args:
        NODE_IMAGE: ${NODE_IMAGE:-docker.m.daocloud.io/library/node:20-bookworm}
    container_name: medusa-frontend
    restart: unless-stopped
    depends_on:
      - medusa-backend
    environment:
      NODE_ENV: development
      MEDUSA_BACKEND_URL: http://medusa-backend:9000
      NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY: auto
      NEXT_PUBLIC_BASE_URL: http://localhost:8000
      NEXT_PUBLIC_DEFAULT_REGION: dk
      NEXT_PUBLIC_STRIPE_KEY: ""
      NEXT_PUBLIC_MEDUSA_PAYMENTS_PUBLISHABLE_KEY: ""
      NEXT_PUBLIC_MEDUSA_PAYMENTS_ACCOUNT_ID: ""
      REVALIDATE_SECRET: supersecret
      CONTENT_ADMIN_SECRET: local-content-secret
    ports:
      - "8000:8000"
    volumes:
      - ./medusa-frontend:/app
      - medusa-frontend-node-modules:/app/node_modules
      - medusa-frontend-next:/app/.next
    command: npm run docker:dev

volumes:
  medusa-db-data:
  medusa-redis-data:
  medusa-backend-node-modules:
  medusa-backend-build:
  medusa-frontend-node-modules:
  medusa-frontend-next:
